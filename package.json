{
  "name": "avail",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/shared"
  ],
  "scripts": {
    "mobile": "npm run start -w packages/mobile",
    "functions:deploy": "npm run deploy -w packages/functions"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.11.0"
  }
}
